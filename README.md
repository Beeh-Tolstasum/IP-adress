# IP Info Tool

A simple Python script to retrieve geographical and network information for any given IP address and display the location on an interactive Folium map.

---

## 🚀 Getting Started

### Prerequisites

To run this script, you need Python and the following libraries installed:

* `requests`
* `pyfiglet`
* `folium`

You can install them using pip:

```bash
pip install requests pyfiglet folium
Run the script:

Bash

python your_script_name.py
Enter the IP address: The script will prompt you to enter the target IP address.

Need a test IP? You can generate a random IP address using this free online tool: https://testingbot.com/free-online-tools/random-ip-generator

View Results:

The information (ISP, Country, City, Coordinates, etc.) will be printed in the console.

An HTML map file named ip_location_xxx.html (the exact name depends on the IP) will be saved in the same directory. Open this file in your browser to see the location.

🇷🇺 Информация на Русском Языке
IP Info Tool
Простой скрипт на Python для получения географической и сетевой информации по заданному IP-адресу с отображением местоположения на интерактивной карте Folium.

🚀 Начало Работы
Требования (Prerequisites)
Для запуска скрипта вам потребуется Python и следующие библиотеки:

requests

pyfiglet

folium

Их можно установить с помощью pip:

Bash

pip install requests pyfiglet folium
Использование
Запустите скрипт:

Bash

python your_script_name.py
Введите IP-адрес: Скрипт предложит вам ввести целевой IP-адрес.

Нужен тестовый IP? Вы можете сгенерировать случайный IP-адрес, используя этот бесплатный онлайн-инструмент: https://testingbot.com/free-online-tools/random-ip-generator

Просмотр Результатов:

Информация (провайдер, страна, город, координаты и т. д.) будет выведена в консоль.

В той же директории будет сохранен HTML-файл с картой под названием ip_location_xxx.html (точное имя зависит от IP). Откройте этот файл в браузере, чтобы увидеть местоположение.
